class defaultconst:

    def __init__(self):
        print("default constructor ")
    #def __init__(self,name):  #it overrides  the previous constructor so it will treated as constructor in class
     #   self.name=name

de=defaultconst()